package com.bankmanagement.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class EnvConfig {
    // Environment variables are now loaded via application.yml with default values
    // or via system environment variables for production deployments
}
